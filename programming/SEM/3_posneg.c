#include <stdio.h>

// pos or neg finder
void main()
{
    int num;
    printf("\nEnter the num :");
    scanf("%d", &num);
    if (num > 0)
        printf("\nPositive number\n");
    else
        printf("\nNegative number\n");
}